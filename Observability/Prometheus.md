Architecture : 
![[Pasted image 20260806115138.png]]


**Prometheus Service Discovery (SD)** is a built-in mechanism that automatically detects, manages, and updates scrape targets in dynamic cloud environments. Instead of manually maintaining a static list of IP addresses in your configuration, Prometheus queries external infrastructure platforms to find endpoints as they scale up or down.